# IMPORTANT: Rules of aumlang(Interpreter)
#
# 2. func parameters are sperated by , : eg func(a,b,c) , no space for now
# 3. keywords -> print, func, getRefCount, return
#    getRefCount -> reference counting garbage collector
#    TODO:
# 4. garbage collection 
let a = 9;
# b = "as a"  # bug here see in func get_num_tokens
# print(a);
a;

let b = 9 + 1;
b;
let add = fn(a, b){
  return a+b;
};

let z = a+b;
let y = add(1,4);
# b = 10 + 11 * 5 + 1;
# print(b9);
let c = true;
if(a > 10){
  a;
}else{
  b;
}
y
# func newFunc(a) {
#   getRefCount a
#   return a+1
# }
# getRefCount a
#
# func multiply(a,b) {
#   return a*b
# }
#
# print newFunc(a)
#
# c = multiply(a,b)
# print c
