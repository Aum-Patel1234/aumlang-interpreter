#include <stdio.h>
#include <stdlib.h>
#include<stddef.h>
#include"../include/eval_expr.h"
#include"../include/vector.h"

typedef struct Token{
    char *expression;
    uint8_t isOperator;
}token;    

int calculate(char* s) {
    char *temp = s;
    vector vec;

    while(temp != '\0'){

    }

    return val;
}