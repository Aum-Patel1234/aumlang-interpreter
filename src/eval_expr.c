#include "../include/vector.h"
#include "../include/eval_expr.h"
#include "../include/token.h"

double calculate(char* s) {
  double ans = 0;
  char* temp = s;

  vector v;
  while (*temp != '\0') {
  }

  return ans;
}
